{
  "name": "newsletter-automation",
  "version": "1.0.0",
  "description": "Static newsletter site build using Firestore instead of MySQL/PHP.",
  "type": "module",
  "scripts": {
    "build:articles": "node scripts/build_articles.js",
    "build": "npm run build:articles"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0"
  }
}
